/*
const fs = require('fs')

fs.readFile('five_letter_words.txt', 'utf8', (err, data) => {
    if (err) throw err;
    
    const lines = '\"[\"' + data + '\"]\"';

    fs.writeFile('test.json', lines, (err) => {
 
        // In case of a error throw err.
        if (err) throw err;
    });
   
});
*/